namespace MasterAPI.Domain.Models
{
    // Esta clase representa los roles de usuario disponibles en la aplicación. 
    // Es decir, define los diferentes tipos de usuarios que pueden existir.
    public static class CustomRol
    {
        public const string ADMIN = nameof(ADMIN);
        public const string CLIENT = nameof(CLIENT);
    }
}